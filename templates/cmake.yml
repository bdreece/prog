---
configure:
  debug:
    - mkdir -p build
    - cmake -DCMAKE_BUILD_TYPE=Debug -B ./build .
  release:
    - mkdir -p build
    - cmake -DCMAKE_BUILD_TYPE=Release -B ./build .
build: cmake --build ./build
test: ctest -V --test-dir=./build/tests
push:
  - git add .
  - git commit
  - git push
